const getTerimaLebur = (state) => state.terimalebur.feedback;
const getErrorTerimaLebur = (state) => state.terimalebur.error;
const getIsEditTerimaLebur = (state) => state.terimalebur.isEdit;
const data = {
  getTerimaLebur,
  getErrorTerimaLebur,
  getIsEditTerimaLebur,
};
export default data;
