const getAllGroupBahan = (state) => state.groupbahan.feedback;
const getErrorGroupBahan = (state) => state.groupbahan.error;
const getIsEditGroupBahan = (state) => state.groupbahan.isEdit;
const data = {
  getAllGroupBahan,
  getErrorGroupBahan,
  getIsEditGroupBahan,
};
export default data;
