const getLoading = (state) => state.ui.loading;
const getBtnLoading = (state) => state.ui.btnLoading;
const getTypeTableLayout = (state) => state.ui.typeTable;
const getLoadPanel = (state) => state.ui.loadpanel;

const data = { getLoading, getBtnLoading, getTypeTableLayout, getLoadPanel };

export default data;
